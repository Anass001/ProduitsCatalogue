<header class="p-3 bg-dark text-white">
    <div class="container">
        <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
            <a href="/" class="d-flex align-items-center mb-2 mb-lg-0 text-white text-decoration-none">
                <svg class="bi me-2" width="40" height="32" role="img" aria-label="Bootstrap">
                    <use xlink:href="#bootstrap"></use>
                </svg>
            </a>

            <nav>
                <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
                    <li><a href="{{ path('app_product') }}"
                           class="nav-link px-2 {% if app.request.attributes.get('_route') == 'app_product' %} text-white {% else %} text-secondary{% endif %}">Produits</a>
                    </li>
                    <li><a href="{{ path('app_category') }}"
                           class="nav-link px-2 {% if app.request.attributes.get('_route') == 'app_category' %} text-white {% else %} text-secondary {% endif %}">Catégories</a>
                    </li>
                </ul>
            </nav>
        </div>
    </div>
</header>