{% extends 'base.html.twig' %}

{% block title %}Ajouter un catégorie{% endblock %}

{% block body %}

    <div class="d-flex justify-content-between align-items-center mt-5 mb-2">
        <h1>Ajouter une catégorie</h1>
    </div>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}

{% endblock %}